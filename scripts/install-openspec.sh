#!/usr/bin/env bash
set -euo pipefail

echo "Install OpenSpec with the version approved by the project:"
echo "  npm install -g @fission-ai/openspec@<pinned-version>"
echo
echo "After installation, verify with:"
echo "  openspec --version"
